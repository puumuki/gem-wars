apply plugin: 'java'

repositories {
    jcenter()
    mavenCentral()
}
sourceSets {
    test {
        java {
            srcDir 'src/test'
        }
    }
}
dependencies {
    compile 'org.slick2d:slick2d-core:1.0.2'
    testCompile "junit:junit:4.+"
}